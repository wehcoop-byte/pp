# Pet Pawtrait – Cloud Run + Firebase Deploy Kit (v2 with fixes)
- Firebase rewrites fixed
- CLIP pipeline typed and weights clarified
- Gemini rationale forces JSON responses
- Dev CORS defaults enabled

See firebase.json, server/* and src/services/geminiService.ts for details.
